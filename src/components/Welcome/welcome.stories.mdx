import { Meta } from '@storybook/addon-docs';

<Meta title="Welcome" />

# 欢迎来到dans-ui
dans-ui 是使用 React Hooks 和 typescript打造的ui框架
### 安装
~~~javascript
npm install dans-ui --save
~~~
### 使用
~~~javascript
// 加载样式
import 'dans-ui/dist/index.css'
// 引入组件
import { Button } from 'dans-ui'
~~~